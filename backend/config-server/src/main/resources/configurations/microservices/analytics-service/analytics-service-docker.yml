server:
  port: 0

eureka:
  instance:
    prefer-ip-address: true
    hostname: ${spring.application.name}
    instance-id: ${spring.application.name}:${random.uuid}
  client:
    service-url:
      defaultZone: http://eureka-discovery:9001/eureka

spring:
#  datasource:
#    url: jdbc:mysql://mysql:3306/db_incident_analytics
#    username: root
#    password: root

  jpa:
    hibernate:
      ddl-auto: update

rabbitmq:
  host: rabbitmq
  port: 5672
  username: guest
  password: guest
  exchange: incident-exchange
  queue-incident-created: incident-created-queue
  routing-key-incident-created: incident.created